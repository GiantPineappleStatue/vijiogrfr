# OpenAI API Key for generating documentation embeddings
# Copy this file to .env and set your API key
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Limit the number of pages to process per source (default: 100)
# EMBED_PAGE_LIMIT=50

# Optional: Process only one type of documentation
# BLENDER_ONLY=true
# AE_ONLY=true
